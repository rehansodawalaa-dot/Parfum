# Backend API (leave empty to use sample data only)
VITE_API_URL=http://localhost:5000/api

# Razorpay public key (safe to expose — it's the publishable key)
VITE_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
